server:
  port: 8083

spring:
  application:
    name:  accounts-mailsender

  mail:
    host: smtp.example.com
    port: 25
    username: username
    password: password
    properties:
      mail:
        smtp:
          auth:  true
          starttls:
            enable:  true
